pip==24.2
setuptools<75
wheel

# ให้ตรงกับ streamlit-authenticator 0.4.1
streamlit==1.37.1
# streamlit 1.37.1 ต้องใช้ rich < 14
rich==13.7.1

# numpy < 2 เพื่อไม่ชนข้อกำหนดของ streamlit
numpy==1.26.4
pandas==2.2.2

qrcode[pil]==7.4.2
fpdf2==2.7.9

gspread==6.1.2
google-auth==2.35.0

# ต้องใช้คู่กับ streamlit >= 1.37.0
streamlit-authenticator==0.4.1

# (เวอร์ชันนี้ตัดตัวคอมโพเนนต์กล้องออกก่อน เราจะใส่ HTML5 กลับให้ทีหลัง)
